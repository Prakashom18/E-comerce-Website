<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <style>
    body {
      background: linear-gradient(135deg, #fef3e7, #d9f99d); /* Orange to Green gradient */
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
    }

    .checkout-container {
      max-width: 900px;
      margin: 50px auto;
      background: #fff;
      border-radius: 25px;
      padding: 30px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.15);
    }

    .product-card {
      display: flex;
      flex-direction: column;
      gap: 20px;
      border-radius: 20px;
      padding: 20px;
      background: linear-gradient(145deg, #fef9f0, #fef3e7);
      box-shadow: 0 10px 25px rgba(0,0,0,0.05);
      transition: all 0.4s ease;
    }

    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    }

    .product-card img {
      border-radius: 20px;
      object-fit: cover;
      max-height: 300px;
    }

    .product-info h3 {
      font-size: 2rem;
      font-weight: 700;
      color: #ea580c;
      margin-bottom: 10px;
    }

    .product-info p {
      font-size: 1.1rem;
      margin: 5px 0;
      color: #16a34a;
    }

    .product-info .total {
      font-size: 1.5rem;
      font-weight: 700;
      margin-top: 10px;
      color: #ea580c;
    }

    .reviews i {
      color: #facc15; /* yellow stars */
    }

    .payment-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      background: linear-gradient(90deg, #34d399, #10b981); /* green gradient */
      color: white;
      font-weight: 700;
      padding: 14px 30px;
      border-radius: 15px;
      font-size: 1.1rem;
      box-shadow: 0 8px 30px rgba(0,0,0,0.2);
      transition: all 0.3s ease;
      margin-top: 20px;
    }

    .payment-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 12px 40px rgba(0,0,0,0.25);
    }

    @media(max-width: 768px){
      .product-card {
        flex-direction: column;
        align-items: center;
        text-align: center;
      }

      .product-card img {
        width: 80%;
        height: auto;
      }

      .product-info h3 {
        font-size: 1.6rem;
      }
    }
  </style>
</head>
<body>

<div class="checkout-container">
  <div class="product-card md:flex md:items-center md:gap-6">
    <!-- Product Image -->
    <div class="w-full md:w-1/2">
      <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
    </div>

    <!-- Product Info -->
    <div class="product-info w-full md:w-1/2 mt-6 md:mt-0">
      <h3><%= product.title %></h3>
      <div class="flex items-center reviews mb-2">
        <i class="fas fa-star"></i>
        <i class="fas fa-star"></i>
        <i class="fas fa-star"></i>
        <i class="far fa-star"></i>
        <i class="far fa-star"></i>
        <span class="ml-2 text-gray-600 text-sm">41 reviews</span>
      </div>
      <p class="text-gray-700"><%= product.description %></p>
      <p class="text-lg font-semibold">Price: Rs. <%= product.price %></p>
      <p class="text-lg font-semibold">Shipping: Rs. <%= shippingCharge %></p>
      <p class="total">Total: Rs. <%= totalAmount %></p>

      <!-- Payment Form -->
      <form action="/esewa/pay-with-esewa" method="GET">
        <input type="hidden" name="price" value="<%= totalAmount %>">
        <input type="hidden" name="oid" value="<%= transaction_uuid %>">
        <button type="submit" class="payment-btn">
          <i class="fas fa-wallet"></i> Pay with eSewa
        </button>
      </form>
    </div>
  </div>
</div>

</body>
</html>
